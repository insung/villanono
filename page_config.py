import streamlit


def add_page_config(st: streamlit):
    st.set_page_config(
        page_title="빌라 실거래 검색은 빌라 노노",
        page_icon="🚀",
        # layout="wide",
        # initial_sidebar_state="expanded",
    )
